name: namer_app
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0 #latest version of http package as of 6/10/2024
  english_words: ^4.0.0
  provider: ^6.1.5
  flutter_dotenv: ^5.1.0 #need for .env file

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
